spring:
  application:
    name: NexPay
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/nexpayDB}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:naman1234}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

vault:
  master-key: dkbrlgh3jna8903fesnfbiou3nnfehafjkeb3iuohjkfbiw946eu

payment:
  simulator:
    poll-interval-ms: 5000
    chaos-mode: NORMAL
    method:
      CARD:
        min-delay-seconds: 2
        max-delay-seconds: 6
        success-rate: 90
      UPI:
        min-delay-seconds: 1
        max-delay-seconds: 3
        success-rate: 90
      NET_BANKING:
        min-delay-seconds: 4
        max-delay-seconds: 10
        success-rate: 80

mock:
  merchantId: 50ea4949-0e2f-40a4-8435-4f4653e4b739